'use strict';

describe('myApp.editWidget module', function() {

  beforeEach(module('myApp.editWidget'));

  describe('editWidget controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var EditWidgetCtrl = $controller('EditWidgetCtrl');
      expect(EditWidgetCtrl).toBeDefined();
    }));

  });
});