'use strict';

describe('myApp.createWidget module', function() {

  beforeEach(module('myApp.createWidget'));

  describe('createWidget controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var createWidgetCtrl = $controller('createWidgetCtrl');
      expect(createWidgetCtrl).toBeDefined();
    }));

  });
});